<Page x:Class="ElectroStore1.Windows.Pages.Catalog"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:ElectroStore1.Pages.MainPages"
      mc:Ignorable="d" 
      d:DesignHeight="600" d:DesignWidth="800"
      Title="Catalog">

    <Grid Background="White">
        <ScrollViewer VerticalScrollBarVisibility="Auto">
            <Grid Margin="20" HorizontalAlignment="Center">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <Label Grid.Row="0" Content="Каталог товаров" 
                       FontSize="24" FontWeight="Bold"
                       Foreground="{StaticResource TextColor}" 
                       Margin="0,10,0,20"
                       HorizontalAlignment="Center"/>

                <ItemsControl Grid.Row="1" x:Name="CategoriesItemsControl">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <WrapPanel Orientation="Horizontal" HorizontalAlignment="Center"/>
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>

                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border Margin="10" Width="220" Height="160" 
                                    BorderBrush="#DDD" BorderThickness="1"
                                    CornerRadius="12" Cursor="Hand"
                                    Background="White"
                                    Padding="15">
                                <Border.Effect>
                                    <DropShadowEffect ShadowDepth="2" Opacity="0.2" BlurRadius="5"/>
                                </Border.Effect>

                                <Border.Style>
                                    <Style TargetType="Border">
                                        <Style.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter Property="Background">
                                                    <Setter.Value>
                                                        <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                                            <GradientStop Color="#F5F5F5" Offset="0"/>
                                                            <GradientStop Color="#FAFAFA" Offset="1"/>
                                                        </LinearGradientBrush>
                                                    </Setter.Value>
                                                </Setter>
                                                <Setter Property="BorderBrush" Value="#03A9F4"/>
                                            </Trigger>
                                        </Style.Triggers>
                                    </Style>
                                </Border.Style>

                                <Grid>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="*"/>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                    </Grid.RowDefinitions>

                                    <TextBlock Grid.Row="0" 
                                             Text="{Binding CategoryName}" 
                                             FontSize="18" 
                                             FontWeight="SemiBold"
                                             HorizontalAlignment="Center" 
                                             VerticalAlignment="Center"
                                             TextWrapping="Wrap"
                                             TextAlignment="Center"
                                             Margin="5"/>

                                    <Rectangle Grid.Row="1" Height="1" Fill="#E0E0E0" Margin="0,10"/>

                                    <TextBlock Grid.Row="2" 
                                             Text="{Binding ProductCount, StringFormat='Товаров: {0}'}" 
                                             FontSize="14" 
                                             Foreground="Gray"
                                             HorizontalAlignment="Center"/>
                                </Grid>

                                <Border.InputBindings>
                                    <MouseBinding MouseAction="LeftClick" 
                                                Command="{Binding DataContext.CategoryClickCommand, 
                                                          RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                                                CommandParameter="{Binding}"/>
                                </Border.InputBindings>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </Grid>
        </ScrollViewer>
    </Grid>
</Page>